
// Process function:
export function ProcessPage({ ...props }) {
    console.log('[Debug Process] rendered:', { props })

    const styles = {
        container: {
            // border: '1px solid red',
        },
    };

    return (
        <>
        <section>

        </section>
        <section >

        </section>
        <section>

        </section>
        </>
    );
}